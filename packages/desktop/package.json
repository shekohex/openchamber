{
  "name": "@openchamber/desktop",
  "version": "1.6.8",
  "private": true,
  "type": "module",
  "desktopPrerequisites": [
    "Rust stable toolchain (via rustup)",
    "Xcode Command Line Tools installed",
    "Tauri CLI installed (cargo install tauri-cli@^2)"
  ],
  "scripts": {
    "tauri": "tauri",
    "tauri:icons": "tauri icon src-tauri/icons/app-icon.manifest.json",
    "tauri:dev": "tauri dev --features devtools",
    "tauri:build": "tauri build",
    "tauri:android:init": "bun run tauri:icons && tauri android init --config src-tauri/tauri.android.conf.json && node ./scripts/apply-mobile-branding.mjs",
    "tauri:android:dev": "bun run tauri:icons && node ./scripts/apply-mobile-branding.mjs && tauri android dev --config src-tauri/tauri.android.conf.json",
    "tauri:android:build": "bun run tauri:icons && node ./scripts/apply-mobile-branding.mjs && tauri android build --config src-tauri/tauri.android.conf.json",
    "tauri:ios:init": "bun run tauri:icons && tauri ios init --config src-tauri/tauri.ios.conf.json && node ./scripts/apply-mobile-branding.mjs",
    "tauri:ios:dev": "bun run tauri:icons && node ./scripts/apply-mobile-branding.mjs && tauri ios dev --config src-tauri/tauri.ios.conf.json",
    "tauri:ios:build": "bun run tauri:icons && node ./scripts/apply-mobile-branding.mjs && tauri ios build --config src-tauri/tauri.ios.conf.json",
    "init:android": "bun run tauri:android:init",
    "dev:android": "bun run tauri:android:dev",
    "build:android": "bun run tauri:android:build",
    "init:ios": "bun run tauri:ios:init",
    "dev:ios": "bun run tauri:ios:dev",
    "build:ios": "bun run tauri:ios:build",
    "build:mobile:web-dist": "node ./scripts/build-mobile-web-dist.mjs",
    "build:sidecar": "node ./scripts/build-sidecar.mjs",
    "build": "bun -e \"process.exit(0)\"",
    "type-check": "bun -e \"process.exit(0)\"",
    "lint": "bun -e \"process.exit(0)\""
  },
  "dependencies": {},
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/node": "^24.3.1",
    "typescript": "~5.8.3"
  }
}
